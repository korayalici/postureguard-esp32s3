# Firmware

This firmware target is the **M5StickS3** based on the **ESP32-S3**.

Firmware implementation and flash steps are **TBD until hardware arrives**. The intended deployment target is an on-device posture classifier using BMI270 sensor input and TensorFlow Lite Micro.
