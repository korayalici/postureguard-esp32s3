# Data

This directory is reserved for posture datasets and recording artifacts.

- `sample/` is kept in version control for tiny example files or schemas.
- `raw/` is intentionally ignored in `.gitignore` for larger local captures.

Do not commit private or sensitive recordings.
